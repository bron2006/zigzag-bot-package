# fly.toml
app = "zigzag-bot-package"

[build]
  dockerfile = "Dockerfile"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0 # Для економії можна поставити 0, або 1 для постійної роботи
  processes = ["app"]

  [[http_service.checks]]
    interval = "20s"
    timeout = "5s"
    method = "GET"
    path = "/"
    grace_period = "90s" # <-- Збільшений час очікування

[[vm]]
  cpu_kind = "shared"
  cpus = 1
  memory_mb = 512

[experimental]
  auto_rollback = true