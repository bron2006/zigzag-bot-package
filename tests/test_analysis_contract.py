# 1. Створюємо папку для тестів та сам файл тесту New-Item -ItemType Directory -Force -Path "tests" $testContent = Get-Clipboard # (Припустимо, ти скопіював код тесту вище) # А краще так, щоб точно: [System.IO.File]::WriteAllText("$(Get-Location)\tests\test_analysis_contract.py", (Get-Clipboard))  # 2. Перевіряємо контракт (якщо видасть OK — ми перемогли) python -m unittest tests/test_analysis_contract.py  # 3. Якщо тест пройшов, робимо фінальний деплой git add . git commit -m "fix: restore contract and add regression test" git push fly deploy --no-cache